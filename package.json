{
  "name": "clotho-monorepo",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "pnpm test:contract && turbo test",
    "test:contract": "node scripts/verify-api-contract.mjs",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.8",
    "typescript": "^5",
    "yaml": "2.9.0"
  }
}
