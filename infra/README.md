# infra/

Deployment and packaging.

| Path | Purpose |
|---|---|
| `docker/` | Dockerfiles per service, used by `docker-compose.dev.yml`. |
| `k8s/` | Kubernetes manifests — **deferred until post-prototype**. |
| `terraform/` | Cloud provisioning — **deferred until post-prototype**. |
