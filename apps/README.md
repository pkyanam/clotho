# apps/

Deployable frontends (pnpm workspace).

| App | Purpose |
|---|---|
| `web` | The main product app shell (Next.js): repo browser, PR view, agent-session presence. |
| `site` | The marketing/teaser site, sharing the same design system (`packages/ui`). |
